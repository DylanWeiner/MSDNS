public static class DNSQuestion {
    static DNSQuestion decodeQuestion(InputStream, DNSMessage) {
        // Read a question from the input stream. Due to compression, you may have to ask the DNSMessage containing this question to read some of
        // the fields.

    }

    void writeBytes(ByteArrayOutputStream, HashMap<String,Integer> domainNameLocations) {

    }

    toString(), equals(), and hashCode() {

    }
}